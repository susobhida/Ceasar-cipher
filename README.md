# Ceasar-cipher
A clean Python implementation of the classic Caesar (shift) cipher with encrypt, decrypt, and brute-force modes.

## How it works

Each letter in the plaintext is shifted forward (encrypt) or backward (decrypt) by a fixed number of positions in the alphabet. Non-alphabetic characters (spaces, punctuation, digits) are left unchanged.
